import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "강의 작성",
};

export default function LectureWriteLayout({ children }: { children: React.ReactNode }) {
  return children;
}
